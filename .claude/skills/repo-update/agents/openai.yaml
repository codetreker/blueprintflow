interface:
  display_name: "repo-update"
  short_description: "Update the Blueprintflow repo"
  default_prompt: "Use $repo-update to change Blueprintflow skills, metadata, docs, CI, scripts, or release files."
