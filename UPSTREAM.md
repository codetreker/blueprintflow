# UPSTREAM — OPC Fork Provenance

BF's `runtime/` and `test/` directories were vendored from OPC at the fork point below. We do not auto-pump from upstream; cherry-picks are documented in the delta log.

## Fork point

- Source: https://github.com/iamtouchskyer/opc (local clone at `/workspace/opc`)
- Commit: bf7910aa38a5310cb8ac392472b87568c5d7c779
- HARNESS_VERSION at fork: 0.10.0 (see runtime/lib/flow-templates.mjs)
- Files vendored verbatim:
  - `bin/opc-harness.mjs` → `runtime/opc-harness.mjs` (entry file renamed to `bf-harness.mjs` in Task 1.4; the rename is recorded in the delta log below)
  - `bin/lib/*.mjs` (40 files) → `runtime/lib/*.mjs`
  - `test/run-all.sh` + `test/test-*.sh` (110 scripts) + `test/test-helpers.sh` + `test/fixtures/`
  - `roles/*.md` (21 files)
  - `pipeline/*.md` (7 files)

Note: the fork plan estimated 111 `test-*.sh` scripts; actual count at the
captured SHA is 110. The plan's count appears to have included `run-all.sh`
or `test-helpers.sh` in the total. No scripts are missing — all `test-*.sh`
files present in upstream were copied.

## Files explicitly NOT vendored

- `bin/opc.mjs` — slash-command dispatcher, replaced by `bf-run` skill (Stage 4 plan)
- `bin/opc-report.mjs` — HTML report, deferred to v2
- `bin/replay-viewer.html`, `bin/replay-open.sh` — deferred to v2
- `bin/hooks/` — PreCompact/PostCompact, deferred to v2

## Delta log

(append one row per BF-side change to vendored files)

| Date | Files | Reason |
|---|---|---|
| 2026-05-17 | (initial vendor) | Fork point captured above |
| 2026-05-17 | roles/*.md (21 files), test/deferred-tests.txt, test/run-all.sh | Vendor OPC roles so harness mandatory-role checks work; add a skip-by-name mechanism to run-all.sh and defer test-install-hooks.sh (depends on bin/opc.mjs which is not vendored). Discovered during Task 1.3 baseline run. |
| 2026-05-17 | runtime/*.mjs (rename map), test/*.sh (rename map), runtime/lib/clean.mjs (BF_DIR_PATTERN) | Brand renames per spec § 'Replaced (brand words)': opc-harness → bf-harness; .harness → .bf (with .harness still recognized for legacy cleanup in clean.mjs); ~/.opc → ~/.bf; opc_compat → bf_compat; OPC_* env vars → BF_*; user-visible help text 'OPC' → 'BF'. Generic vocabulary (flow / node / verdict / handshake / gate / route / transition / synthesize / FLOW_TEMPLATES / HARNESS_VERSION) kept. Suite remains at 108 passed / 0 failed / 1 deferred. |
| 2026-05-17 | repo layout (runtime/→bin/, core/→references/, plugins/bf/ → root), +package.json +SKILL.md +scripts/postinstall.mjs +bin/bf.mjs +pipeline/.gitkeep | Reorganized from plugin form to bare-skill + npm form (@codetreker/bf). Mirrors OPC's distribution model. .claude-plugin/marketplace.json no longer registers bf — npm is the only distribution channel. Tests still 108/0/1 under new layout. |
| 2026-05-17 | pipeline/{gate-protocol,handoff-template,criteria-lint,report-format,context-brief,role-evaluator-prompt,evaluator-prompt}.md, pipeline/README.md | Stage 3: vendor 7 Core node protocols from OPC pipeline/. Pack-specific protocols (implementer-prompt, executor-protocol, discussion-protocol, test-design-protocol, ux-*) intentionally not vendored — they belong inside each Pack's protocols/ folder. |
| 2026-05-17 | roles/* (9 kept, 11 moved to packs/product-engineering/roles/, 1 deleted), test/test-guardrails.sh (fixture rename) | Stage 3 task 3.4: triage of 21 vendored OPC roles per docs/specs/2026-05-16-bf-fork-design/opc-role-mapping.md. Core retains: planner, architect, tester, security, a11y, compliance, devil-advocate, skeptic-owner, user-simulator. Pack receives: pm, designer, frontend, backend, devops, mobile, engineer, dd-engineer, new-user, active-user, churned-user. Deleted: investor (not in BF's path). test-guardrails.sh fixtures swapped engineer/frontend → tester/security (the original choice was incidental; mandatory-role enforcement mechanism is what's under test). |
| 2026-05-17 | bin/lib/{flow-escape,runbooks,loop-advance}.mjs | Stage 4 task 4.1: sweep 9 deferred `/opc <verb>` user-facing strings to `/bf <verb>`. The literal word "OPC" referring to the upstream project name is unchanged. |
| 2026-05-17 | bin/lib/{util,flow-core,flow-transition,viz-commands}.mjs, packs/product-engineering/{pack.json,protocols/*.md}, test/{run-all.sh,test-seal-advance.sh,harness-hardening/test-{a,b,c,d,e,f}-*.sh} | Stage 4 task 4.2: harden bf-harness per Stage 3 demo findings. Six sub-tasks, each TDD'd: (a) 998abe2 widen `--dir` sandbox to accept `$TMPDIR/bf-*`; (b) d108969 seal auto-creates `nodes/<id>/run_1/` and hard-fails on empty artifact set; (c) 708e14b inference regex accepts bare `eval.md` (`^eval(-.+)?\.md$`) + 4 Pack protocols document the `eval-<role>.md` convention; (d) c7dd08b seal returns `sealed:false` when `validationErrors` non-empty (test-seal-advance group 1 fixture updated — the old lenient sealed:true on validator errors was a footgun); (e) 7bedcb0 Pack-overridable `mandatory_roles` via new `--pack` flag, product-engineering pack.json declares `["skeptic-owner"]` explicitly; (f) 5dc70dc viz renders all non-PASS back-edges per node (was: dropped second back-edge when both FAIL and ITERATE were present). Suite: 108 → 114 passed / 0 failed. |
| 2026-05-18 | bin/lib/dispatcher/{arg-parser,pack-discovery,wo-resolver,flow-selector}.mjs, test/dispatcher/test-{arg-parser,pack-discovery,wo-resolver,flow-selector}.sh, test/run-all.sh | Stage 4 task 4.3: dispatcher scaffold — 4 small, independently-testable modules built TDD. (a) f95017b arg-parser splits argv into `{verb,args,flags,knownVerb}` with 18-verb KNOWN_VERBS set + kebab→camel flag keys + `--help`/empty → `verb:"help"`; (b) 1c72e15 pack-discovery scans repo-local `packs/*/pack.json` (v0.2 scope; sibling npm packages deferred to v0.3+); (c) 3da08cb wo-resolver walks slash-separated WO id under `BF_WO_HOME` (default `~/.bf/wo/`) validating each segment has `wo.md`, returns frontmatter `{schema,current_state,pack}`; (d) 2596e76 flow-selector reads `routing[schema,state]` from pack manifest with `state_aliases` applied. run-all.sh now also recurses into `test/dispatcher/`. Suite: 114 → 118 passed / 0 failed. |
| 2026-05-18 | bin/lib/dispatcher/{node-runner,flow-runner}.mjs, bin/lib/verbs/{create,brainstorm,breakdown,loop,close,execute,_single-flow}.mjs, bin/bf.mjs (rewritten from Stage 1+2 placeholder), test/dispatcher/test-node-runner.sh, test/verbs/test-{create,brainstorm,breakdown,loop,close,execute-leaf}.sh, test/run-all.sh | Stage 4 task 4.4: lifecycle verbs. (a) `node-runner.mjs` ticks one flow node — v0.2 stubs the agent dispatch by emitting `eval-<role>.md` (verdict PASS) for each required role of the node, then calls `seal` + `transition --pack`; Stage 5 swaps in real subagent dispatch via Claude Code subagents. (b) `create` scaffolds `~/.bf/wo/<id>/wo.md` at state `new` (honors `BF_WO_HOME`); `bf.mjs` rewritten as a thin dispatcher (parse → `import('./lib/verbs/<verb>.mjs')`, ~15 lines). (c) 4 single-flow verbs share `_single-flow.mjs` + `flow-runner.mjs` (init harness → walk nodes → finalize → rewrite `current_state` from `flow.produces.desired_state`); `loop` is the special case — refuses with `deferred:true` + Stage-5 hint, exit 0. (d) `execute` orchestrator loops `selectFlow → runFlowToCompletion` until `current_state == done`; v0.2 has a `task,shaped → null` routing gap that the smoke test documents via an explicit `sed -i` flip (Stage 5 owns the breakdown step for leaf tasks). Suite: 118 → 125 passed / 0 failed (+1 node-runner, +1 create, +4 single-flow verbs, +1 execute orchestrator). |
| 2026-05-18 | bin/lib/verbs/{show,tree,list,discard,escape,pack,flow,help}.mjs, bin/lib/dispatcher/wo-resolver.mjs (+`listWos()` + WO_HOME export), bin/bf.mjs (escape-verb routing), test/verbs/test-{show-tree-list,escape,pack-flow,help}.sh | Stage 4 task 4.5: inspection / escape / meta verbs (12 verbs in 3 sub-commits). (a) `show` dumps frontmatter+body+`## Recent runs`; `tree` walks `BF_WO_HOME` recursively with `--all` to surface terminal-state WOs; `list` is the flat form with `--pack`/`--state`/`--schema` filters; `discard` rm-rfs a WO dir behind `--force`. Shared `listWos()` helper added to `wo-resolver.mjs`. (b) `escape.mjs` is a thin forwarder: resolves the WO's most recent `runs/run-*/` and spawns `node bin/bf-harness.mjs <skip|pass|stop|goto> --dir <runDir>`. `bin/bf.mjs` routes the 5 escape verbs through `escape.mjs` via a `Set`. `resume` is a v0.2 marker (full implementation deferred to Stage 5 — needs cross-WO active-run discovery). (c) `pack` exposes `list`/`info <id>`; `flow` exposes `list [<pack>]`/`viz <flow-id>` (`viz` forwards to harness `viz --flow-file`); `help` reads a single `VERB_DOCS` table so the catalog and per-verb usage stay in sync. Suite: 125 → 129 passed / 0 failed (+1 test-show-tree-list, +1 test-escape, +1 test-pack-flow, +1 test-help). |
| 2026-05-18 | bin/lib/dispatcher/nl-parse.mjs, bin/bf.mjs (unknown-verb routing), test/verbs/test-nl-parse-stub.sh | Stage 4 task 4.6: NL parse front-end (deterministic v0.2). New `transcribeDeterministic(argv)` returns `{verb,args,flags,source:"verb-match"}` when the first token (case-insensitively) matches the 18 KNOWN_VERBS — auto-corrects mangled casing; otherwise `{verb:null,source:"needs-llm"}`. `bin/bf.mjs` splits its old `help||!knownVerb` branch: `help` keeps its existing path; `!knownVerb` now routes through `nl-parse` → re-dispatches on a verb-match (escape verbs go via `escape.mjs`) or emits a JSON `error` pointing at `bf help` and exits 2. LLM-driven free-form transcription is explicitly deferred to Stage 5 (the surrounding Claude Code skill will wrap the null case). Suite: 129 → 130 passed / 0 failed (+1 test-nl-parse-stub). | (a) `show` dumps frontmatter+body+`## Recent runs`; `tree` walks `BF_WO_HOME` recursively with `--all` to surface terminal-state WOs; `list` is the flat form with `--pack`/`--state`/`--schema` filters; `discard` rm-rfs a WO dir behind `--force`. Shared `listWos()` helper added to `wo-resolver.mjs`. (b) `escape.mjs` is a thin forwarder: resolves the WO's most recent `runs/run-*/` and spawns `node bin/bf-harness.mjs <skip|pass|stop|goto> --dir <runDir>`. `bin/bf.mjs` routes the 5 escape verbs through `escape.mjs` via a `Set`. `resume` is a v0.2 marker (full implementation deferred to Stage 5 — needs cross-WO active-run discovery). (c) `pack` exposes `list`/`info <id>`; `flow` exposes `list [<pack>]`/`viz <flow-id>` (`viz` forwards to harness `viz --flow-file`); `help` reads a single `VERB_DOCS` table so the catalog and per-verb usage stay in sync. Suite: 125 → 129 passed / 0 failed (+1 test-show-tree-list, +1 test-escape, +1 test-pack-flow, +1 test-help). |
| 2026-05-18 | package.json (0.1.0-alpha → 0.2.0-alpha + publishConfig.access=public), README.md (install + usage tour rewrite), test/test-package-dryrun.sh | Stage 4 task 4.7: packaging dry-run + README rewrite. (a) bump version to mark the dispatcher landing; add `publishConfig.access=public` so the first `npm publish` doesn't need an explicit flag; existing `files` allowlist already covers `bin/`, `packs/`, `pipeline/`, `roles/`, `references/`, `SKILL.md`, `scripts/`, `test/`, `UPSTREAM.md`, `README.md` so the new `bin/lib/verbs/` and `bin/lib/dispatcher/` trees ride along automatically. `test/test-package-dryrun.sh` asserts the tarball file list — must include every key path; must exclude `node_modules`, `.bf-demo`, `.harness/`. Tarball: 473.5 kB, 340 files. (b) README install/use rewrite: real `npm install -g` path + 18-verb usage tour + honest v0.2 status (stub agents, `loop` deferred, NL stub, not-yet-published). Suite: 130 → 131 passed / 0 failed (+1 test-package-dryrun). |
| 2026-05-18 | SKILL.md (executable orchestrator), bin/lib/dispatcher/node-runner.mjs (`agents-needed` envelope + `BF_RESUME_NODE`), bin/lib/verbs/execute.mjs, bin/lib/util.mjs (~/.bf/wo sandbox), bin/lib/verbs/version.mjs, test/test-skill-orchestrator-shape.sh, test/harness-hardening/test-g-dir-wo-home.sh, test/verbs/test-version.sh, docs/specs/2026-05-18-stage-5-demo/{brainstorm,close-leaf}-run.md, docs/specs/2026-05-18-stage-5-demo-trace.md | Stage 5 wrap. (5.1 commit 188a578) SKILL.md becomes an executable orchestrator; node-runner under `BF_ORCHESTRATOR=skill` returns `{status:"agents-needed", roles, runDir, expectedArtifacts, ...}` instead of stub-emitting; orchestrator spawns one Agent subagent per role, then re-invokes with `BF_RESUME_NODE=<id>` to seal+transition. (5.2 commit 148ae92) Real brainstorm-task demo: fresh `add-bf-version-verb-...` WO driven new → shaped with sonnet subagents (planner/architect/engineer/tester). (5.3 commit 8d85fba) Real close-leaf-task demo: same WO doing → done; `implement` subagent shipped real code at a202ef8 (`bf version` verb + test). In-demo fix 49e3f94 widened `--dir` sandbox to `~/.bf/wo/`. (5.4) Top-level demo trace + 8-item Stage 6 input list: findings #2/#3/#5 → code/config fixes in 6.1; #4 → doc clarification; #1 already closed; #6/#7/#8 informational. Suite: 131 → 135 passed / 0 failed at end of Stage 5. |
| 2026-05-18 | packs/product-engineering/pack.json (+`task,shaped → close-leaf-task`), bin/lib/dispatcher/node-runner.mjs (gate auto-synth before orchestrator branch), bin/lib/criteria-lint.mjs (BF Pack section-name aliases + checklist → OUT-N derivation), references/{work-object,flow,gate,wo-home,pack,README}.md (v0.3 markers + 2 behavioral clarifications), docs/specs/2026-05-18-stage-6-core-v0.3.md, test/harness-hardening/test-{h-gate-auto-synthesize,i-criteria-lint-bf-sections}.sh, test/verbs/test-execute-leaf.sh (single-call traversal), test/test-stage4-regression.sh (tree --all) | Stage 6 task 6.1: Core contracts → v0.3 lifting 3 actionable + 2 doc findings from Stage 5 demo. (Finding #2) criteria-lint accepts BF Pack section names (Objective / Boundary / Acceptance criteria); checklist items auto-derive OUT-N records for vague/impossible/uniqueness checks; OPC-only checks (verification-mapped, quality-section) skip on BF-shaped inputs. (Finding #3) node-runner's `nodeType === "gate"` branch precedes the orchestrator branch — gate nodes emit a synthetic `eval-gate.md` and fall through to seal; transition still enforces verdict math against upstream handshakes. (Finding #5) product-eng routing gains `task,shaped → close-leaf-task` so leaf tasks skip the no-op breakdown step (`execute` on a fresh `task,new` WO now drives new → done in one call under stubs). (Finding #4) `references/work-object.md` clarifies: acceptance checklist is a static record, not a live toggle; state machine is the acceptance truth. All 5 contracts marked v0.3. Suite: 135 → 137 passed / 0 failed (+ test-h gate auto-synth, + test-i lint BF sections; test-execute-leaf + test-stage4-regression updated for new routing). |
| 2026-05-18 | MIGRATION.md, README.md (rc.1 status + MIGRATION link), package.json (0.2.0-alpha → 1.0.0-rc.1), plugins/blueprintflow/skills/{bf-runtime-adapter,bf-teamlead-role-reminder,bf-teamlead-slow-cron-checkin}/ DELETED, docs/specs/2026-05-18-stage-5-6-retro.md | Stage 6 wrap (tasks 6.2/6.3/6.4). (6.2 commit 6c33358) MIGRATION.md lifted from `bf-skill-migration.md`; README install section points at it. (6.3 commit a26e5bc) 3 v6 skills retired per migration table (bf-runtime-adapter, bf-teamlead-role-reminder, bf-teamlead-slow-cron-checkin); historical copies preserved under packs/product-engineering/reference-v6/. (6.4) Version bump 0.2.0-alpha → 1.0.0-rc.1; README rewritten to declare rc.1 status with the demo-passed callout and v0.3 contract pointer; npm pack --dry-run clean at 480.5 kB; Stage 5+6 retro doc records the v1 release-candidate posture and the post-v1 deferral list. Suite: 137/0; test-stage4-regression PASS. |

## Deferred to Stage 4 (bf-run skill)

(Section closed: Stage 4 swept all 9 deferred `/opc <verb>` user-facing
strings to `/bf <verb>` in commit 3f155a7. Search to confirm later:
`grep -rn '/opc ' bin/lib/`.)
