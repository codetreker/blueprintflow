# Core Contracts v0.3 — Change Log

> Lifted from Stage 5 demo findings
> (`docs/specs/2026-05-18-stage-5-demo-trace.md` § "Stage 6 input list").
> Companion to `references/*.md` v0.3.

## Summary

- 8 Stage 5 findings → 3 code/config fixes + 2 doc clarifications +
  3 informational (no separate action)
- 0 breaking contract changes
- 1 additive Pack-config change (`task,shaped → close-leaf-task`)
- 2 behavioral clarifications in `references/` (no schema change)

## Per-finding traceback

### Finding 1 — `--dir` sandbox missed `~/.bf/wo/`
- **Source:** brainstorm-run.md § Findings #1
- **Status:** closed in-demo at commit `49e3f94`
- **Code affected:** `bin/lib/util.mjs`
- **Contract change:** none (mechanical fix)
- **Breaking?** No

### Finding 2 — `criteria-lint` rejected BF Pack section names
- **Source:** brainstorm-run.md § Findings #2 (criteria-lint node)
- **Severity:** clarification
- **Code affected:** `bin/lib/criteria-lint.mjs`
- **Change applied:** Pack section names (`Objective` / `Boundary` /
  `Acceptance criteria`) are accepted alongside OPC names
  (`Outcomes` / `Verification` / `Quality Constraints` / `Out of
  Scope`). Acceptance-criteria checklist (`- [ ]` items) auto-derives
  OUT-N records for vague-word / impossible-to-fail / uniqueness
  checks. OPC-only checks (verification-mapped, quality-section) skip
  when the input is BF-shaped.
- **Test added:** `test/harness-hardening/test-i-criteria-lint-bf-sections.sh`
- **Breaking?** No — OPC-shaped inputs continue to lint identically.

### Finding 3 — Gate-type nodes incorrectly requested agents
- **Source:** brainstorm-run.md § Findings #3 (re-confirmed in
  close-leaf-run.md § Findings #7)
- **Severity:** important
- **Code affected:** `bin/lib/dispatcher/node-runner.mjs`
- **Contract clarified:** `references/flow.md` (gate nodes
  auto-synthesize from upstream handshakes; orchestrators MUST NOT
  request agents)
- **Change applied:** in `node-runner.mjs`, the `nodeType === "gate"`
  branch precedes the `BF_ORCHESTRATOR=skill` branch. Gate nodes emit
  a synthetic `eval-gate.md` and fall through to seal; the `transition`
  verb still enforces verdict math against upstream handshakes, so a
  gate over a failing upstream remains blocked.
- **Test added:** `test/harness-hardening/test-h-gate-auto-synthesize.sh`
- **Breaking?** No — orchestrators that previously branched on a gate
  `agents-needed` envelope simply never see that envelope now.

### Finding 4 — Acceptance-criteria checklist not toggled to `[x]`
- **Source:** brainstorm-run.md § Findings #4 (re-confirmed in
  close-leaf-run.md § Findings #8)
- **Severity:** clarification
- **Contract clarified:** `references/work-object.md` § "Open
  questions" — the `current_state` field IS the acceptance signal; the
  `## Acceptance criteria` checklist is the static record of what
  `done` was defined to mean, NOT a live toggle.
- **Code change:** none
- **Breaking?** No

### Finding 5 — Routing gap `task,shaped → ?`
- **Source:** close-leaf-run.md § Findings #5
- **Severity:** clarification (with real friction)
- **Config affected:** `packs/product-engineering/pack.json`
- **Contract clarified:** `references/pack.md` § "Open questions" —
  Packs whose leaf schema does not break down further SHOULD route
  `<schema>,shaped → <close-leaf flow>` directly.
- **Change applied:** added `"task,shaped": "close-leaf-task"` to the
  product-engineering Pack's `routing` map. `bf execute` on a fresh
  `task,new` WO now drives `new → shaped → doing → done` in a single
  invocation (under stub agents) or one continuous orchestrator loop
  (under `BF_ORCHESTRATOR=skill`).
- **Test updated:** `test/verbs/test-execute-leaf.sh` reflects single-
  call traversal; `test/test-stage4-regression.sh` uses `tree --all`
  since the regression WO now finishes at `done`.
- **Breaking?** No — adds a new routing rule; the previous "stuck
  with no flow for `task,shaped`" outcome is no longer reachable.

### Finding 6 — Demo pollutes the demo-runner's repo
- **Source:** close-leaf-run.md § Findings #6
- **Severity:** nice-to-have
- **Action:** none for v1; documented in `2026-05-18-stage-5-6-retro.md`.
- **Breaking?** No

### Finding 7 — Gate node redundancy (re-confirmation of #3)
- No separate action; subsumed by Finding 3 fix.

### Finding 8 — No criteria-toggling (re-confirmation of #4)
- No separate action; subsumed by Finding 4 clarification.

## Contracts not behaviorally changed

- `references/gate.md` — already documented mechanical synthesis; v0.3
  bumped for consistency.
- `references/wo-home.md` — Stage 5 didn't exercise; remains v0.2
  behavior, now versioned v0.3 only for consistency.
