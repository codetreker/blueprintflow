interface:
  display_name: "bf-runtime-adapter"
  short_description: "Blueprintflow: Map to runtime"
  default_prompt: "Use $bf-runtime-adapter to check the current agent runtime and team capabilities."
