interface:
  display_name: "bf-task-execute"
  short_description: "Blueprintflow: Orchestrate one task to accepted"
  default_prompt: "Use $bf-task-execute to create or resume one concrete task and advance it through implementation, review, merge, and acceptance."
