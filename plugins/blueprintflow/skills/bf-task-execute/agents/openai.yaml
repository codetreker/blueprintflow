interface:
  display_name: "bf-task-execute"
  short_description: "Blueprintflow: Orchestrate one task to accepted"
  default_prompt: "Use $bf-task-execute to create or resume a concrete task from milestone readiness, then advance it through implementation, review, merge, and acceptance."
